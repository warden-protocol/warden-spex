{
  "name": "warden-spex",
  "version": "0.0.1",
  "description": "Statistical Proof of Execution (SPEX) by Warden Labs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "ts-node src/main.ts",
    "dev": "ts-node-dev --respawn src/main.ts",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "test": "echo \"No tests yet\"",
    "demo": "ts-node src/main.ts demo"
  },
  "dependencies": {
    "bloom-filters": "^3.0.4",
    "dotenv": "^17.2.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
